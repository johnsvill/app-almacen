@model IEnumerable<AppWebPersonal.Models.TelefonoCliente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeroTelefono)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroTelefono)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nit)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodigoTelefono">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodigoTelefono">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodigoTelefono">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
